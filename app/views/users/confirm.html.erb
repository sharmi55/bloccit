<h1>Sign Up Confirmation</h1>

  <div class="row>
    <div class="col-md-8">
      <h2>Are you sure these values are correct?</h3>
      <h3>Email:<%= @user.email %></h3>
      <h3>Email:<%= @user.name %></h3>
    </div>
  </div>

  <div class="row>
    <div class="form-group">
      <%= button_to "Yes", {controller: "users", action: "create", params: params}, class: 'btn btn-success' %>
      <%= button_to "No", {controller: "users", action: "new", class: 'btn btn-default' %>
    </div>
  </div>
